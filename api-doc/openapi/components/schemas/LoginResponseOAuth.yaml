type: object
properties:
 token:
 type: string
 description: >-
 Authorization JWT token.
 type: object
 two_fa:
 type: object
 description: null when used with the Proxiport Plus OAuth plugin
description: Response returned by the `/oauth/login` endpoint when Proxiport Plus OAuth is enabled
